# OpenClaw Skill Source

这个目录是 OpenClaw 技能源码集合，由原根目录 `skills/` 重命名而来，用来和 `claude-skills/`、`codex-skills/` 明确区分。

## 使用说明

- 单个技能源码位于 `openclaw-skill/<skill-name>/`。
- GitHub 文档链接统一使用 `openclaw-skill/<skill-name>`。
- 打包后的 zip 内部仍可能包含 `skills/` 目录，这是 OpenClaw workspace 的安装目录结构，不代表仓库源码目录仍叫 `skills/`。
